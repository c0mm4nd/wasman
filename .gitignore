.idea
.vscode
*.wasm
/wasman

# but keep the test fixtures compiled from tracked .wat sources with the
# official wat2wasm, so CI (which has no wabt) reads the golden binaries
# rather than compiling them with the module under test
!/bench/testdata/*.wasm
!/testdata/*.wasm

# generated by spectest/gen.sh (official wasm suite manifests + binaries)
/spectest/testdata/
